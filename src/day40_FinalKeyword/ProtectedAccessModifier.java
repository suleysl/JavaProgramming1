package day40_FinalKeyword;

public class ProtectedAccessModifier {

    static String name1  = "cydeo";  //default is not visible outside the package

    protected static String name2 = "Wooden spoon"; // it could be visible outside the package if the class is sub-class(child class).
}
